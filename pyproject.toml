[project]
name = "lorekeeper"
version = "0.1.0"
description = "Data Specialist AI agent — answers data questions with production-grade SQL via MCP"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "anthropic>=0.52",
    "httpx>=0.27",
    "mcp>=1.9",
    "pydantic>=2.11",
    "pydantic-settings>=2.9",
    "python-dotenv>=1.1",
    "rich>=13.9",
    "trino>=0.330",
    "typer>=0.15",
    "pyyaml>=6.0",
]

[dependency-groups]
dev = [
    "pytest>=8.3",
    "pytest-asyncio>=0.25",
    "ruff>=0.11",
]

[project.scripts]
lorekeeper = "lorekeeper.cli:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["lorekeeper"]

[tool.ruff]
target-version = "py313"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[tool.ruff.format]
quote-style = "double"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
